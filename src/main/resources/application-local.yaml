spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sofia?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: sofia
    password: sofia
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
